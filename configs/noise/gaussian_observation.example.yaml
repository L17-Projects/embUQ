schema_version: "1.0"
kind: noise
metadata:
  id: gaussian_observation
  name: Gaussian Observation Noise
spec:
  family: gaussian
  likelihood:
    stage: M2
    components:
      - additive_noise
      - relative_noise
  parameters:
    - sigma
  bounds:
    sigma:
      - 0.0
      - 1.0
