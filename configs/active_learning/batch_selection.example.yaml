schema_version: "1.0"
kind: active_learning
metadata:
  id: batch_selection
  name: Batch Selection
spec:
  acquisition: qei
  batch_size: 8
  candidate_source: emb_compression_reference
  refresh_policy: refresh_or_archive
