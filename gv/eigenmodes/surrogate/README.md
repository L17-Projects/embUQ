GV eigenmodes surrogate placeholder.

This directory is reserved for eigenmodes-specific surrogate fixtures, manifests, and small artifact descriptors. Shared surrogate implementation belongs under `src/meso_uq`.
