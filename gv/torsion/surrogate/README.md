GV torsion surrogate placeholder.

This directory is reserved for torsion-specific surrogate fixtures, manifests, and small artifact descriptors. Shared surrogate implementation belongs under `src/meso_uq`.
