GV buckling evalkit placeholder.

This directory is reserved for buckling-specific evaluation fixtures, manifests, and small reproducibility assets. Shared evaluation implementation belongs under `src/meso_uq`.
