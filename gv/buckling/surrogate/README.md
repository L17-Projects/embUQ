GV buckling surrogate placeholder.

This directory is reserved for buckling-specific surrogate fixtures, manifests, and small artifact descriptors. Shared surrogate implementation belongs under `src/meso_uq`.
