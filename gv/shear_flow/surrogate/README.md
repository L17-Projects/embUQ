GV shear-flow surrogate placeholder.

This directory is reserved for shear-flow-specific surrogate fixtures, manifests, and small artifact descriptors. Shared surrogate implementation belongs under `src/meso_uq`.
