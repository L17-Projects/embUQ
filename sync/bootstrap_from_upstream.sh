#!/usr/bin/env bash
set -euo pipefail

echo "Bootstrap imports are tracked through SYNC_MANIFEST.yaml."
echo "Follow-up PRs will implement the actual copy and import steps from the upstream research repository."
